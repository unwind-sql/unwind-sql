"""Static SPA assets served by `unwind.web` (HTML, JS, CSS).

This directory is overwritten by the Vite build at `web-client/`. The
`__init__.py` itself is shipped via `web-client/public/` so it survives
`emptyOutDir: true`.
"""
